# 安全与隐私

Userese Video 是个人本地工作台。默认绑定本机地址；跨设备访问请绑定具体的私有网络地址，并仅与受信任设备共享访问链接。服务不支持直接暴露为公网多租户应用。

每次启动生成随机访问口令。网页使用 URL fragment 接收口令，建立 HttpOnly、SameSite=Strict 会话后从地址栏移除。媒体与项目 API 都需要登录；文件路由使用白名单，校验 Host 与写请求 Origin，并限制 JSON 请求体大小。内置 HTTP 服务没有 TLS，跨设备请通过 Tailscale 等加密私网使用。访问同一工作台的浏览器拥有相同编辑权限。

程序不会主动上传用户媒体。可选 faster-whisper 首次使用会下载模型；核心审片、编码和演示不需要网络。应用页面没有第三方脚本、字体请求、统计或遥测。README 的 GitHub 徽章不属于运行中的工作台。

原片复制进项目，编码输出去除原始元数据与章节；画面、录音、转写内容本身仍可能包含隐私。完整交付 ZIP 包含转写和决定证据，不能当作纯公开视频包随意分享。

本地项目视为用户可信输入。避免打开陌生人提供的项目目录或在共享可写目录中运行工作台；本软件不提供对恶意本地协作者的文件系统隔离。FFmpeg 处理不可信媒体时也应保持系统依赖更新。

报告安全问题时，请先使用 GitHub 的私密漏洞报告功能（若仓库已启用），不要在公开 Issue 附带凭据或私有素材。普通功能缺陷可在 Issues 中使用合成样例报告。
